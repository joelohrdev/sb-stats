models:
  Pitching:
    date: date
    inning: integer
    balls: integer
    strikes: integer
    outcome: string
    note: text

  Hitting:
    date: date
    inning: integer
    balls: integer
    strikes: integer
    outcome: string
    note: text
